{
  "name": "nimora",
  "private": true,
  "version": "1.0.0",
  "description": "Team project and task management application",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run db:setup --prefix backend",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
